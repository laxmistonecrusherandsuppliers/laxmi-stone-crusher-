const router=require('express').Router();const c=require('../controllers/attendance.controller');const {auth,requireAdmin}=require('../middleware/auth');router.use(auth);router.get('/staff',c.staff);router.post('/staff',requireAdmin,c.addStaff);router.get('/',c.listAttendance);router.post('/',c.saveAttendance);router.get('/advances',c.advances);router.post('/advances',c.addAdvance);router.get('/leaves',c.leaves);router.post('/leaves',c.addLeave);module.exports=router;
